---
description: Switch to another CTF challenge
allowed-tools: Bash(./tools/ctfctl *)
---

Resolve `$ARGUMENTS` to an existing challenge with `./tools/ctfctl use '...'`, then show `./tools/ctfctl status --markdown`. If ambiguous, list matching challenges and ask the operator to choose.
